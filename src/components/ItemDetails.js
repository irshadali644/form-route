function ItemDetails(props) {
    return (
        <div>
            <p> {props.id} Routing allows us to configure an app that accepts various URLs and is mapped to specific components. Once the matching URL is found, then the matching page/component will be rendered into the HTML DOM</p>
            <p>Routing allows us to configure an app that accepts various URLs and is mapped to specific components. Once the matching URL is found, then the matching page/component will be rendered into the HTML DOM</p>
            <p>Price: <strong>$100</strong></p>
            <button>Back</button>
        </div>
    )
}
export default ItemDetails;